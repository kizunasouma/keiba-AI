name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Lint & 型チェック
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python 3.13 セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Poetry インストール
        run: pip install poetry

      - name: 依存関係インストール
        run: poetry install --with dev
        working-directory: .

      - name: Ruff リンター実行
        run: poetry run ruff check backend/
        continue-on-error: true

      - name: Ruff フォーマットチェック
        run: poetry run ruff format --check backend/
        continue-on-error: true

  test-unit:
    name: ユニットテスト（DB不要）
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python 3.13 セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Poetry インストール
        run: pip install poetry

      - name: 依存関係インストール
        run: poetry install --with dev

      - name: ユニットテスト実行（DB不要のテストのみ）
        run: poetry run python -m pytest backend/tests/test_features.py -v --cov=backend/app --cov-report=xml
        env:
          DATABASE_URL: "postgresql://dummy:dummy@localhost:5432/dummy"

      - name: カバレッジレポートをアップロード
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-unit
          path: coverage.xml

  test-integration:
    name: 結合テスト（PostgreSQL使用）
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password_ci
          POSTGRES_DB: keiba_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Python 3.13 セットアップ
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Poetry インストール
        run: pip install poetry

      - name: 依存関係インストール
        run: poetry install --with dev

      - name: DBマイグレーション実行
        run: poetry run alembic upgrade head
        working-directory: backend
        env:
          DATABASE_URL: "postgresql://test_user:test_password_ci@localhost:5432/keiba_test"

      - name: 結合テスト実行
        run: poetry run python -m pytest backend/tests/ -v --ignore=backend/tests/test_features.py --cov=backend/app --cov-report=xml
        env:
          DATABASE_URL: "postgresql://test_user:test_password_ci@localhost:5432/keiba_test"

      - name: カバレッジレポートをアップロード
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-integration
          path: coverage.xml

  frontend-check:
    name: フロントエンドチェック
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "frontend/package-lock.json"

      - name: 依存関係インストール
        run: npm ci

      - name: TypeScriptコンパイルチェック
        run: npx tsc --noEmit

      - name: ビルド
        run: npm run build
